export interface LevelRateOrientationStructureClasses {
    /** The classes to apply in the mainContainer */
    mainStarsContainer: string;
    /** Classes to apply in the main DOM element of every star content */
    starContainer: string;
}
